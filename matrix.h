#ifndef MATRIX_H
#define MATRIX_H
void inMatrix(int **t, size_t m, size_t n);
void outMatrix(const int * const * t, size_t m, size_t n);
void deleteMatrix(int **t, size_t m);
void **createMatrix(size_t m; size_t n);
#endif
